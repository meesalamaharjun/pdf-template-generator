<section class="page">
  <header class="page-header justifyEnd">
    <img
      class="byjus-logo"
      style="height:100px"
      src="https://pdf-template-generation.s3.ap-south-1.amazonaws.com/assess/images/byjus_logo.png"
    />
  </header>
  <section class="section">
    <div class="section-heading">
      <h3>LEARNING PATTERN ANALYSIS</h3>
    </div>
    <div class="section-content">
      <p>
        This section of the report will help you in understanding your potential
        skills and abilities. It will further help you to identify the skill
        areas which you can develop, in order to be proficient in your chosen
        career path
      </p>
    </div>
    <div class="section-circle">
      {{#with learning1}}
        {{#each subjectnames}}
          <div class="circle-{{this.count}} learning-circle">
            <span class="inner-1 learning-inner">{{this.name}}</span>
            <span class="inner-2 learning-inner">{{printValue
                this.name
                @root
                'percentage'
              }}%</span>
          </div>
        {{/each}}
      {{/with}}
    </div>
    <div class="section-content">
      {{#with learning1}}
        {{#each points}}
          <p>
            {{this.title}}
            <span
              class="{{printValue this.name @root 'value'}} bold-font"
            >({{printValue this.name @root 'value'}}):</span>
            {{this.description}}
          </p>
        {{/each}}
      {{/with}}
    </div>
  </section>
</section>